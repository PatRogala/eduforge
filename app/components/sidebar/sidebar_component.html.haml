-# The sidebar container is now clean white with a subtle border, matching the overall app chrome.
%aside.bg-white.transition-all.duration-300.overflow-hidden.min-h-screen{"data-sidebar-target": "aside"}
  .h-full.flex.flex-col.py-4
    %nav.flex-1.px-4
      %ul.space-y-1
        %span.px-3.my-3.text-xs.font-semibold.uppercase.text-neutral-500{"data-sidebar-target": "category"}= t(".navigation.main")
        - public_links.each do |link|
          %li
            = render_link(link)
        - if logged_in?
          %span.px-3.my-3.text-xs.font-semibold.uppercase.text-neutral-500{"data-sidebar-target": "category"}= t(".navigation.personal")
        - logged_in_links.each do |link|
          %li
            = render_link(link)
        - if instructor?
          %span.px-3.my-3.text-xs.font-semibold.uppercase.text-neutral-500{"data-sidebar-target": "category"}= t(".navigation.instructor")
          - instructor_links.each do |link|
            %li
              = render_link(link)
