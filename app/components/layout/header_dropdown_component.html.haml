.absolute.right-0.mt-2.w-56.bg-white.rounded-lg.shadow-lg.ring-1.ring-black.ring-opacity-5.animate-fadeIn.z-50
  -# User Information Header
  .px-4.py-3.border-b.border-neutral-200
    %p.text-sm.font-semibold.text-black.truncate= current_user.email
    - roles_display = current_user.roles.map(&:name).map(&:titleize).join(', ')
    - if roles_display.present?
      %p.text-xs.text-neutral-500.mt-1= roles_display

  -# Navigation Links
  .py-2
    = link_to edit_user_registration_path, class: "group w-full px-4 py-2 text-sm text-neutral-700 hover:text-black flex items-center" do
      %span.mr-3>
        = lucide_icon("settings-2", class: "h-4 w-4 text-neutral-400 group-hover:text-black")
      = t(".account_settings")

  -# Destructive Action
  .py-2.border-t.border-neutral-100
    = button_to destroy_user_session_path, method: :delete, class: "group w-full px-4 py-2 text-sm text-red-600 hover:text-red-900 flex items-center cursor-pointer" do
      %span.mr-3>
        = lucide_icon("log-out", class: "h-4 w-4 text-red-600 group-hover:text-red-900")
      = t(".log_out")
